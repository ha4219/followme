import styled from "@emotion/styled";

export const SignupContainer = styled("div")`
  display: flex;
  padding: 1rem;
  width: 100vw;

  & .label {
    font-family: paybooc-Bold;
    font-size: 1rem;
    align-self: center;
    width: 120px;
  }

  & .textInput {
    width: 240px;
    height: 40px;
  }

  & .btn {
    margin-left: 1rem;
    color: #ffffff;
  }
`;
