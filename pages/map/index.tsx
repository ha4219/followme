import MapContainer from "@components/MapContainer";
import { Container } from "@mui/material";

const Map = () => {
  return (
    <Container>
      <MapContainer />
    </Container>
  );
};

export default Map;
